			vTemp = etiquetaHtml("<caption",vTemp);
			vTemp = etiquetaHtml("<th",vTemp);
			vTemp = etiquetaHtml("<th",vTemp);
			vTemp = etiquetaHtml("<td",vTemp);
			vTemp = etiquetaHtml("<td",vTemp);






/*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*//*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*/
/*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*//*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*/
/*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*//*Se crea una funcion para simplificar el uso de de las etiquetas por medio de la concatenacion*/


function etiquetaHtml(etiqHtml,variableTemp1) /*Se reciben 2 parametros - parte de la equtiqueta HTML y el contenido almacenado temporalmente en la vTemp para proceder a insertarlo en el codigo*/
		{
			var contador1 = variableTemp1.indexOf(etiqHtml+" style="); /*Se concatena la etiqueta*/
			if( contador1>-1 )
			{
				var variableTemp2 = variableTemp1.substring(contador1);

				var contador2 = contador1+variableTemp2.indexOf(";\">");

				var str = variableTemp1.substring(contador1,contador2+3);

				variableTemp1 = variableTemp1.split(str).join(etiqHtml+">");
			}
			return variableTemp1;
		}